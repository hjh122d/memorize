import styled from "styled-components";

const Container = styled.div`
  text-align: center;

  Button {
    display: block;
    margin: 8px auto;
  }
`;

const Styles = {
  Container,
};

export default Styles;
